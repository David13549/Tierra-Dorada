[phases.setup]
aptPkgs = ["chromium"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "/usr/bin/chromium"

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"
